syntax="proto3";

message BasestationManualDrive {
    sint32 forward_backward = 1; // Forward/backward speed command (positive = forward, negative = backward)
    sint32 turn = 2; // Left/right turn command (positive = right, negative = left)
}